services:
  db:
    image: postgres:17
    container_name: anti-db
    ports:
      - "5435:5432"
    environment:
      POSTGRES_USER: test-user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: anti_db
